1. Clone the repository - git clone https://github.com/HasinPramodya/issue-tracker-assignment-frontend.git

2. install dependencies - npm install

3. set up environment variable by creating new .env file

4. build the project - npm run build

5. run the projrct - npm run dev
